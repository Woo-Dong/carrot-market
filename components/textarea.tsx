import { cls } from "../libs/utils";
interface TextAreaPros {
  label?: string;
  name?: string;
  [key: string]: any;
}
export default function TextArea({ label, name, ...rest }: TextAreaPros) {
  return <div></div>;
}
